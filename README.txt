Learning exercise in Haskell.
Reads price quotes from a pcap file and streams them to output.


build:

    $ cabal build

run:

    $ cabal run

view help and command line options:

    $ cabal run cs -- -h
